package ex03_number;

public class Ex03 {

	public static void main(String[] args) {
		
		// 대입 연산
		int score = 100; // 등호(=)가 대입 연산자이다. 같다 라는 뜻이 아님.
		// int score <- 100; // 100을 스코어에 보내서 저장하기. 100을 데이터베이스로 이동시켜줌.
		// 100 <- score는 불가능함. 애초에 100은 변수가 아님.
		System.out.println(score);
		
		// 연습
		// x에 10이 있고, y에 20이 있다.
		// x와 y에 저장된 값을 서로 교환하시오.
		int x = 10;
		int y = 20;
		int temp;
		
		temp = x; // temp <- x 로 백업
		x = y; // x <- y 로 전송
		y = temp; // y <- temp 값
		
		System.out.println(x);
		System.out.println(y);
		
		// 복합 대입 연산자
		// +=, -=, *=, /=, %= 등
		int wallet = 0;
		wallet = wallet + 5000;
		// = 
		wallet += 5000; // wallet = wallet + 5000; //자주 사용되는 연산 // wallet에 5000원을 더해라
		wallet -= 3000; // wallet = wallet - 5000;
		System.out.println(wallet);

		// 연습문제
		// 통장 잔액(balance)에서 이자 5%를 받았음을 나타내자.
		long balance = 10000;
		balance %= 150;
		System.out.println(balance);
		
		
		
	}

}
